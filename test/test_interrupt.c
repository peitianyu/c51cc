/*=============================*
 * 中断函数测试
 *=============================*/

/*=============================*
 * 一、基本中断函数测试
 *=============================*/

// 外部中断0 (中断号0，使用寄存器组0)
void interrupt_func(0, 0) {
    int flag = 1;
}

// 定时器0中断 (中断号1，使用寄存器组1)
void interrupt_func(1, 1) {
    int count = 0;
    count = count + 1;
}

/*=============================*
 * 二、不同寄存器组测试
 *=============================*/

// 外部中断1 (中断号2，使用寄存器组2)
void interrupt_func(2, 2) {
    int val = 100;
}

// 定时器1中断 (中断号3，使用寄存器组3)
void interrupt_func(3, 3) {
    int value = 200;
}

/*=============================*
 * 三、带复杂逻辑的中断函数
 *=============================*/

// 串口中断 (中断号4，使用寄存器组0)
void interrupt_func(4, 0) {
    int i;
    int sum = 0;
    for (i = 0; i < 10; i = i + 1) {
        sum = sum + i;
    }
}

/*=============================*
 * 四、普通函数与中断函数混合
 *=============================*/

int normal_function(int a, int b) {
    return a + b;
}

// 定时器2中断 (中断号5，使用寄存器组1)
void interrupt_func(5, 1) {
    int result;
    result = normal_function(1, 2);
}

/*=============================*
 * 五、主函数
 *=============================*/

int main() {
    int a = 0;
    return a;
}
