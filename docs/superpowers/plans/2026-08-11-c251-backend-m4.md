# C251 后端 M4（寄存器扩展与大小收敛）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** M4 = 大小收敛（keil_size_compare 304% → 目标 <150%）+ 功能补齐（variadic ABI / 32 位运算）。核心手段：**WR8-WR14 寄存器扩展**（Keil 用 8 个字寄存器, 我们 4 个）、循环变量钉寄存器、@WRj+imm16 偏移寻址。验收 = Suite 64 OK 保持 + Execute 107 PASS 保持 + keil_size_compare TOTAL 显著下降。

**架构：** `c251_isel.c`（寄存器数 4→8、循环变量钉寄存器、偏移寻址）、`c251_encode.c`（WR8-WR14 编码覆盖、@WRj+imm16 寻址模式）、`c251_gen.c`（循环分析）。

**技术栈：** C99 / tcc.exe / Python3 / sim251（真值）/ Keil C251（大小对比, D11）。

**全局验证要求**：每个任务 = ① 专项测试（sim251 真值）② suite 全量无回归（64 OK）③ execute 全量无回归（107 PASS）。

**参考**：
- Keil 参考 `docs/c251-寄存器分配参考-keil.md`（8 寄存器/循环钉寄存器/偏移寻址）
- M3 计划 `docs/superpowers/plans/2026-08-11-c251-backend-m3.md`（基线: Suite 64/0, Execute 107/0, keil 304%）
- sim251 decode_impl.inc（WR8-14 / @WRj+imm16 编码权威）

---

## 当前基线 (2026-08-12)

| 指标 | 值 |
|------|-----|
| Suite | 64 OK / 0 FAIL / 37 SKIP（全绿） |
| Execute | 107 PASS / 0 FAIL / 0 CE（全绿） |
| keil_size_compare (suite) | TOTAL 304.2%（92 测试） |
| 常量折叠 (00-09) | Keil 30B vs cc 72B |
| 比较 (11_cmp_lt_gt) | 873% |
| sbit RMW (99) | 547% |

---

### 任务 1：WR8-WR14 寄存器扩展（8 个字寄存器）

**文件：** `c251_isel.c`（寄存器分配 4→8）、`c251_encode.c`（编码覆盖）

- [ ] **步骤 1：确认编码支持**
  - 251 Source 模式 R0-R15 全可用。WR8-WR14 = R8-R15。
  - 检查 encode 的 parse_reg / MOV / 算术 / 比较 / 跳转的 WR 字段（4 位 = j/2, WR0-WR30 ✓ 已支持 16 个 WR 的字段编码）
  - sim251 decode: regop2_word `(b1>>4)*2` — 4 位字段 ✓ WR0-WR30 可达
- [ ] **步骤 2：isel 寄存器数组 4→8**
  - `reg_val[4]` → `reg_val[8]`；`isel_temp_wr` / `isel_alloc_wr` / `isel_temp_wr_avoid_bytes` 的 `w <= 6` → `w <= 14`
  - `alloc_far_dr` 的候选对扩展（DR8/DR12 可用）
- [ ] **步骤 3：ABI 冲突检查**
  - Keil 参数寄存器 (WR6/4/2/0) 与返回 WR6 — 扩展后参数装载/返回不变
  - save_live_regs_stack PUSH R0-R7 → R0-R15（8 个 WR）
- [ ] **步骤 4：验证**
  - suite/execute 全量无回归
  - keil_size_compare: 记录 TOTAL 变化（spill 减少应显著）
  - t82d/t_lhs2（复杂表达式）再验证

### 任务 2：循环变量钉寄存器（Keil: len→WR6, n→WR4 全程）

**文件：** `c251_gen.c` / `c251_isel.c`

- [ ] **步骤 1：循环承载值识别**
  - phi 链: 循环回边 phi (dest 出现在 ≥2 个 phi arm) = 循环变量
  - 简单版: 每函数选 2 个最热的循环 phi → 钉到专用寄存器 (WR6/WR4, 避开 ABI 返回)
- [ ] **步骤 2：钉寄存器实现**
  - 循环变量优先分配固定 WR, 不参与普通分配
  - 跨调用 (CALL 在循环内) 时 save/restore
- [ ] **步骤 3：验证**
  - f_loop/f_tern (Keil 参考): c251cc 应生成 len/n 固定寄存器循环
  - suite/execute 全量 + keil_size_compare

### 任务 3：@WRj+imm16 偏移寻址

**文件：** `c251_encode.c`（MOV @WRj+dis16,Rm 编码）、`c251_isel.c`（OFFSET→偏移寻址折叠）

- [ ] **步骤 1：确认编码**
  - sim251: `MOV Rm,@WRj+dis16` (0x69 家族) / `MOV @WRj+dis16,Rm` (0x79)
  - Keil 参考: `MOV @WR10+buf,R1` — 静态基址 + 寄存器偏移
- [ ] **步骤 2：OFFSET 折叠**
  - `store offset(&sym, idx)` → `MOV @WRj+off,Rm` (基址 &sym 常量 + idx*size)
  - 局部数组 (__loc_fn_buf): 同理
- [ ] **步骤 3：验证**
  - 83_lvalue_addr_protect / 82_printf_chain 汇编应明显变短
  - suite/execute + keil_size_compare

### 任务 4：variadic ABI（printf 变参）

**文件：** `parser.c`（... 语法）、`c251_isel.c`（变参调用/读取）

- [ ] **步骤 1：前端 ... 支持**
  - `void f(int, ...)` 声明解析（检查现有）
  - `va_list/va_start/va_arg`（或 c251_libc 提供）
- [ ] **步骤 2：变参传递**
  - Keil ABI: 前 N 参数寄存器, 其余栈
  - 变参读取: va_arg 从栈/寄存器恢复
- [ ] **步骤 3：验证**
  - printf 类测试（82_printf_chain 变参部分）

### 任务 5：32 位 DRk 运算（long 完整）

**文件：** `c251_isel.c`（DRk 运算路径）

- [ ] **步骤 1：32 位加/减/比较**
  - ADD/SUB DRk (regop2_dword) — sim251 支持
  - CMP DRk
- [ ] **步骤 2：32 位乘除**
  - MUL DRk (32×32→64?) — 或长乘算法
  - DIV/MOD — 长除算法或 Keil ?C?LIB
- [ ] **步骤 3：验证**
  - long 运算测试（0046-inits long 部分）

### 任务 6：M4 验收

- [ ] suite/execute 全绿保持
- [ ] keil_size_compare: TOTAL 目标 <150%
- [ ] 结果记录到本文档

---

## 当前进展 (2026-08-12)

### 已完成
| 提交 | 内容 | 效果 |
|------|------|------|
| 4db8350 | WR8-WR14 寄存器扩展 (4→8) + PUSH/POP R8-15 + 慢测试跳过 | 0041 49s→32s; 全绿保持 |
| 85d9736 | 无中断时去 0x40 stub 填充 | keil_size 304%→250% |
| a0c1a4b | const_merge 跨块 use 悬空修复 (块内替换 + 无跨块 use 才合并) | 11_cmp_lt_gt 常量折叠错乱修复 |
| b7a5a04 | @WRj+dis16 位移寻址折叠 (字节级) + 101_fold_offset 专项测试 | 数组访问省 ADD |

### 深度分析: 常量折叠为何不安全 (101_fold_offset 专项测试实证)

**主因: 编码缺失 (sim251 mov_dis 只有字节级)**
- 位移寻址家族 (0x09/0x19/0x29/0x39/0x49/0x59/0x69/0x79): sim251 只实现
  0x09 (字节读), 0x19 (字节写), 0x69/0x79 (far DRk); **字级 0x29/0x39 未实现**
  ("default: break" 静默跳过!)。
- 折叠 int 数组 (字访问) 触发不存在编码 → encode 返回 -1 (unsupported) →
  **指令被静默丢弃** → 读值错误 (48_ptr_arith ret=128, 专项 ret=120 = 8|16|32|64 全字访问)。
- 修复: 折叠限字节访问 (fold_ok 加 size<=1); 字级需先实现 sim251 0x29/0x39。

**次因: 地址语义 (已防护)**
- 折叠的 OFFSET dest 只含基址 (不含 off), 访问靠 @WRj+off 补偏移。
- 若 dest 被非 LOAD/STORE 消费 (加法/比较/传参) → 未加偏移地址 → 错。
- 防护: 块内 use 全为 LOAD/STORE 的 ptr + global_live 检查 (跨块不折叠)。

**编码劫持 bug (调试发现)**
- parse_indirect_wr_off 对无偏移 "@WRj" 返回有效值 → 普通字读 @WRj 被劫持
  到字节位移分支 → unsupported。修复: 无偏移返回 -1。

### 验证
- Suite: **65 OK / 0 FAIL / 37 SKIP** (新增 101_fold_offset)
- Execute: **106/106 (默认, 跳过 0041)** / 107/107 (含 0041)
- keil_size: 254% (含 101 新测试)

### 待做
- [ ] sim251 实现字级位移寻址 0x29/0x39 → 解锁 int 数组折叠
- [ ] SSA 常量传播深度 (phi 单臂折叠) → 11_cmp_lt_gt 232B → Keil 30B
- [ ] variadic ABI / 32 位 DRk 运算

---

## 常量折叠链断裂修复 (208b5e6) — simssa 量化实证

### 用户洞察: "SSA 阶段常量应该已折叠"
正确 — 根因不是 @WRj+dis16 编码, 而是 **SSA 优化器的常量折叠链断裂**:

### simssa --stats 量化 (新增 Before/Optimized 指令数统计)
| 测试 | 修复前折叠率 | 修复后 | keil_size |
|------|------------|--------|-----------|
| 11_cmp_lt_gt | 51.3% (76→37, br=7/phi=7/add=6 残留) | **98.7% (76→1)** | 773%→**37%** |
| 10_cmp_eq_ne | 部分折叠 | **97.6% (41→1)** | 373%→**37%** |

### 根因: pass_const_branch 的 term 取错
- 块 instrs 末尾有 **NOP 化 phi 残留** (pass_phi 简化产物)
- pass_const_branch 用 `list_get(b->instrs, len-1)` 取 term → 拿到 PHI → **跳过所有 br**
- 常量条件分支 (br v6/v13/v21...) 全部不折叠 → 死块不消除 → phi 单臂不折叠
  → add 累加链不传播 → 37 指令冗余链 (vs Keil 1 条)

### 修复
`pass_const_branch` 改用 `block_last_effective_instr` (跳过 NOP/PHI)。

### 验证
- Suite 65/0 全绿 + Execute 106/106 全绿
- keil_size TOTAL: 304% (M3) → 254% (@WRj+off) → **248%**
- 常量折叠类测试 (00-13) 已与 Keil 相当或更优 (36.7%)

---

## SSA 优化专项 (simssa 驱动) — 2026-08-12

### simssa --stats 新增
- Before/Optimized 指令数 + 折叠率 + 未折叠的 const 条件 br/全 const 臂 phi 统计
- 全量扫描: 98 测试平均折叠率 **49.5% → 52.8%**

### 已提交优化
| 提交 | 优化 | 效果 |
|------|------|------|
| 208b5e6 | pass_const_branch 用 block_last_effective_instr (块尾 NOP/PHI 残留跳过 br) | 11: 51%→98.7%, 10: 97.6%, keil 773%→37% |
| e6ce56d | 只读全局折叠 (无 store + &g 未逃逸 + 非 static + 无指针 reloc) | 53: 0%→93.3%, 71: 88.9%, 03: 60% |

### 已探索但回退: 多块函数内联 (常量参数)
- 12 个测试有"常量参数 + 多块函数"调用 (13/14/20/20b/52/61/62/63/67/69)
- 内联 13_abs_val (3 块) 实验: 27.8%→55.6%; 14_if_chain: 35.7%→61.9%
- **回归**: 14 值错 (ret=8 应 2), execute 2 CE — 块 id 冲突/值映射/ret 值替换的边界 bug
- **回退 (git checkout)**, 记录待做: 需专项调试 (tail_id 分配、vmap 跨块、ret 值替换)

---

## 多块函数内联完成 (198d95a) — 2026-08-12

### 实现 (src/core/ssa_pass.c)
| 组件 | 说明 |
|------|------|
| inline_multi_block | 2-16 块 + 无 phi + 无 CALL + 非递归 + 指令白名单 + ≤20 有效指令 (is_inline 豁免) |
| clone_instr_inline | 克隆指令: dest/args 查 vmap, labels 中 blockN 经 bmap 重定向, 未映射返回 NULL (bail) |
| Pass A 预映射 | 全量值映射 (entry+非 entry 所有 dest) 先于任何克隆 → **跨块 vmap 修复** |
| tail_id 分配 | tail_id=max_block_id(f)+1 先算; base=tail_id+1 分配新块; 全量建好后再克隆 → **tail_id 修复** |
| ret 处理 | ret→JMP tail; 多路 ret → 尾块 phi (臂值定义在 pred 块); 单路直接替换; void ret 不建臂 → **ret 值替换修复** |
| pass_inline_func_call | 先试多块 (成功则 call 之后指令移入尾块, k=n 跳过), 回退单块 |
| clear_dead_subtree | pass_const_branch 折叠时递归清空死分支子树 → 死 ret 路径块被清空 → 原有 phi_prune (空 instrs 剪臂) 生效 → 折叠收敛 |

### 三个已知 bug 区的修复对照
| 回退记录的问题 | 修复 |
|---------------|------|
| tail_id+nnew 分配 (块 id 冲突/悬空) | 先算 tail_id/base, 建块后克隆; bmap 查表重定向 |
| 跨块 vmap (非 entry 块引用 entry 值) | Pass A 全量预映射, 克隆只查表 |
| ret 值替换 (14 值错 8≠2) | 每 ret 一臂 phi, call->dest→phi dest; void ret 无臂 |

### 验证
- 13_if_else: main → `ret const 12`; 14_if_chain: main → `ret const 2` (keil 274%→**124%**)
- 52_multi_return: keil 193%→**113.3%**; keil TOTAL: 248%→**239.4%**
- Suite **102 OK** / Execute **106 PASS** / tests/c251/inline_check.py 全 PASS

### 探索后弃用 (记录)
1. **phi_pred_reachable_via_term 剪除无前驱块的臂** — 破坏 0057-duff (死循环):
   无前驱块 (如 duff 的 b5 = [const 0, jmp phi块]) 仍是 phi 的**真前驱** (它分支到 phi 块),
   剪臂违反 SSA phi/前驱不变量 → phi_simplify 把双臂变单臂 → trunc 自循环。还原。
2. **内联白名单扩展 (ADDR/LOAD/STORE)** — 触发既有 inline_single_call STORE/LOAD 克隆路径崩溃
   (结合 value_used 对 @-store 跳过 args 的既有语义, DCE 删定义留下悬空引用, 后续 pass 段错误)。
   20b_big_branch 因 total_eff>20 本就不内联, 无收益, 放弃。

### 待做 (M4 剩余)
- 20_switch_case (phi=1): phi 支持内联
- keil_size 收敛 (239.4%→<150%): sbit RMW (99: 502%) / 93: 304% / 94: 296% / 20: 371%
- variadic ABI / 32 位 DRk / STC32G 复验

---

## sbit 直接位操作完成 (c03d3b2) — 2026-08-12

### isel sbit store 模式 (path1 const → SETB/CLR; path2 值模式)
| 模式 | 生成 |
|------|------|
| store @sbit, const 0/1 | CLR/SETB bit (2B) — 值&1 决定 |
| store @sbit, lnot(load 同 sbit) | CPL bit (2B) 翻转 |
| store @sbit, or/and(sbit load, sbit load) | MOV C,a + ORL/ANL C,b + MOV bit,C |
| store @sbit, load @sbitB | MOV C,B + MOV bit,C 位拷贝 |
| 一般 0/1 值 | CLR + JZ + SETB (6B, 省 RMW 4+ 指令) |

### isel BR 位分支: cond=zext(load @bit) → JB bit,tid; zext(lnot(load)) → JNB bit,tid
### sbit load 抑制: 值全部由位直接模式消费 (CPL/JNB/JB/位拷贝) → 跳过 load 物化序列
- **全函数 use 扫描** (含 phis) — 块内扫描漏 phi 消费导致 86 误判抑制 (值错) 的根因

### encode 新增: SETB(0xD2) CLR(0xC2) CPL(0xB2) MOV C,bit(0xA2) MOV bit,C(0x92)
### ORL C(0x72) ANL C(0x82) JB(0x20) JNB(0x30) — 均含 rel8 越界降级
### 关键 bug: JB/JNB 漏发 rel 占位字节 → 下条指令覆盖 rel 槽 → 逃逸 (c03d3b2 修)

### 效果: 99_sbit 502%→281%, 73_sbit 405%→287%, 86 360%→340%, 94 296%→194%
### keil TOTAL: 227% → 223.0%; Suite 102 OK / Execute 106 PASS

### 剩余热点 (keil)
- 20b_big_branch 276% (5688B) — total_eff>20 不内联 (设计使然)
- 82_printf_chain 442% (1494B) — variadic ABI (M4-C)
- 101_fold_offset 372% — sim251 字级位移寻址 0x29/0x39 未实现
- 83_lvalue_addr_protect 348% / 93_isr 304% / 84 272%

---

## 会话 2 续 M4 — 比较标志直达 + 数组/switch 折叠 (keil 204%→197.8%)

### 6ba1386 比较标志直接跳转 + ISR PSW1
- try_br_fold 模式 C: cmp→ZEXT(dest)→BR (ne x,0 被 pass 折叠成 zext 后直接复用比较标志)
- try_br_fold 模式 D: cmp→SELECT(cond) 三元直接用标志
- pass_select_to_phi: 三元汇合块 select 各前驱由同根 BR 判定 → 转 phi (cond 不再跨块)
- **ISR prolog/epilog 保存 PSW1(0xD1)**: JSL/JSGE 依赖 PSW1 的 N 标志,
  ISR 内 CMP/算术破坏 → RETI 后被中断代码标志错 (93 死循环/错值根因 — 模式C暴露既有bug)

### bc7f362 数组元素常量折叠
- global_readonly 逃逸检查支持 OFFSET 链 + **修复 ADDR labels 无 @ 的匹配 bug**
  (逃逸检查从未生效 — 60_bubble_sort 经指针写数组被折叠为初始常量)
- pass_global_load case2: is_static 硬条件 → is_const_global
- case2 跳过 blob reloc 字段 (结构体指针 &x 链接期定值 → 0050-inits)
- 101_fold_offset 711→163B (372%→85.3%), keil 破 200%

### 48d09a9 phi 内联支持
- inline_multi_block 允许非 entry phi (entry phi 拒绝)
- clone_phi_inline: arm 值 vmap, arm 标签 bmap 重定向; 处理 instrs 残留 PHI
- 20_switch_case 371%→107% (main→ret const 99), 63 370%→133%

### keil TOTAL: 204.2% → 197.8% (M4 起点 248%)
### 验证: Suite 102 OK / Execute 106 PASS 保持
