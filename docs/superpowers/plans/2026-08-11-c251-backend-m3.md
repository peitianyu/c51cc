# C251 后端 M3（sfr/sbit/bit/中断/far/存储类）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）逐任务实现此计划。步骤使用复选框（`- [ ]`）语法跟踪进度。

**目标：** M3 = 设计规格（`docs/superpowers/specs/2026-08-11-c251-backend-design.md` §10）中 **M4 外设与中断（sfr/sbit/中断）提前实施** + **M3 XSMALL 剩余（far 指针/存储类）**，并修复 M2.5 遗留的循环后置递减与前端缺口。验收 = test/suite 72-99（M3 专项 27 个）**全绿** + test/execute 104→107 PASS + Keil 对齐。

**架构：** 在 M2.5 基础上扩展：`c251_gen.c`（process_global_var 按存储类选段、中断向量表）、`c251_isel.c`（sfr/sbit 直读直写、far 24 位指针、idata/xdata 寻址、MOVX）、`c251_encode.c`（MOVX 编码、far 地址 reloc）、`src/core/parser.c`（far 关键字、中断后缀语法）。

**技术栈：** C99 / tcc.exe / Python3 / sim251（真值）/ Keil C251（大小对比，D11）。

**全局验证要求（用户指定）**：每个任务的验收 = ① 该任务专项测试（sim251 真值）② **test/suite 全量回归**（`python scripts/c251/c251_sim.py`，应保持 55 OK 无回归或记录新增 OK）③ execute 相关测试。三项齐备才算任务完成。

**参考**：
- 设计规格 §10 里程碑表（M3/M4 定义）+ 风险 §11（REGPARMS 传参实证、far 空间模型、编码表交叉验证）
- M2.5 计划 `docs/superpowers/plans/2026-08-11-c251-backend-m25.md`（遗留清单：sfr/sbit、中断、far、32 位 DRk、variadic）
- 指令集权威：`docs/stc32g_指令功能排序.md` + `sim251/tests/functional.py` + `sim251/src/decode_impl.inc`
- sim251 mcs251.h：中断向量（VEC_T0=0x0B 等，sim251/tests/periph/）与 far 内存路由（XFR 0x7EFE 窗口）
- D10 全大端 / D11 Keil 参考 / D8 精简

---

## 文件结构

**修改（src/core/c251/）**：
- `c251_gen.c` — process_global_var 按存储类选段（SEC_DATA/IDATA/XDATA）、中断向量表 0x0003+N*8
- `c251_isel.c` — sfr/sbit 直读直写（已完成）、far 24 位指针、idata/xdata 寻址路径、MOVX
- `c251_encode.c` — MOVX 编码、far 地址 reloc、新寻址模式编码
- `c251_libc.c` — （按需）xdata/far 库例程

**修改（src/core/）**：
- `parser.c` — `far` 关键字解析（C51: `type far *` / `far type *`）、中断后缀语法（已完成）
- `ssa.c` — far 指针类型传播（24 位指针 size=3）

**修改（scripts/c251/）**：
- `c251_sim.py` — （按需）far/xdata 内存路由断言支持

---

### 任务 1：far 关键字 + 24 位指针（77_far_mem）✅ 方向锁定

**文件：**
- 修改：`src/core/parser.c`（far 关键字 → CTYPE_FAR 属性 / 指针 size=3）
- 修改：`src/core/c251/c251_gen.c`（far 全局 → 专用段 + 基址）
- 修改：`src/core/c251/c251_isel.c`（far LOAD/STORE：24 位地址装载 DRk + @DRk 间接）

- [ ] **步骤 1：确认 sim251 far 内存路由（先读再写）**

```bash
# 77_far_mem.c 依赖: far 扩展 SFR (XFR 0x7EFE 窗口, 0x7efe10 = P0PU) + far xram (0x000100)
# 读 sim251/tests/periph/ + decode_impl.inc 确认:
#   - far 读: 24 位地址 → 内存路由到 XFR/xram 的指令序列
#   - 24 位指针装载: MOV DRk,#imm24 编码 (或 WR 高字 + 空间字节)
grep -n "0x7efe\|XFR\|far" sim251/src/*.c sim251/tests/*.py | head -20
```

- [ ] **步骤 2：parser 添加 far**

`far` 关键字解析（C51 两种位置：`unsigned char volatile far *` 与 `far unsigned char *`）→ 指针类型 size=3（1 空间字节 + 2 地址字节, Keil 通用指针布局）；`ssa.c` 指针传播沿用 ctype_data 模式（line 999 已有继承逻辑）。

- [ ] **步骤 3：far 全局变量段**

`process_global_var` 对 far 全局 → 独立 section（xram 区, 基址按 sim251 映射）, 符号注册 + reloc。

- [ ] **步骤 4：far LOAD/STORE isel**

`MOV WRj,@DRk`（24 位间接）/ `MOV @DRk,WRj`；far 常量地址 `(unsigned char volatile far *)0x7efe10` → IROP_INTTOPTR 24 位常量 → 直接寻址序列。

- [ ] **步骤 5：验证**

```bash
python scripts/c251/c251_sim.py test/suite/77_far_mem.c   # 期望 OK (0)
```

- [ ] **步骤 6：Commit**

```bash
git add src/core/parser.c src/core/c251/ src/core/ssa.c
git commit -m "feat(c251): far 24 位指针 — far 关键字 + XFR/xram 寻址 (77_far PASS)"
```

---

### 任务 2：存储类 data/idata/xdata（87_idata / 88_xdata_arrays / 96_idata_ptr）

**文件：**
- 修改：`src/core/c251/c251_gen.c`（按 ctype_data 选段: data→SEC_DATA@低地址, idata→SEC_IDATA, xdata→SEC_XDATA）
- 修改：`src/core/c251/c251_isel.c`（idata/xdata 符号寻址路径 + 指针间接路径按存储类分流）
- 修改：`src/core/c251/c251_encode.c`（MOVX 编码: 0xE0/0xF0 @DPTR + 0xE2/0xE3/0xF2/0xF3 @Ri 已在 sim251, 251 扩展形态查 decode_impl.inc）

- [ ] **步骤 1：确认当前根因**

`process_global_var`（c251_gen.c:143）全放 SEC_EDATA（基址 0x80），`ctype_data` 属性 parser 已解析（parser.c:2006-2011, data=1/idata=2/pdata=3/xdata=4/edata=5/code=6）但 gen 未用。87_idata ret=2（iarr[0] 写读不一致）, 88_xdata ret=2（arr[0] 失败）, 96_idata_ptr ret=1（*p 读错）。

```bash
scripts/c251cc.exe -ssa test/suite/87_idata.c | head -40   # 看 idata 变量 SSA 形态
scripts/c251cc.exe -asm test/suite/88_xdata_arrays.c 2>&1 | head -30   # 看当前寻址
```

- [ ] **步骤 2：gen 按存储类选段**

```c
/* c251_gen.c process_global_var 伪代码 */
int cls = att.a.ctype_data;   /* 0=默认→EDATA, 1=data→DATA, 2=idata→IDATA, 4=xdata→XDATA */
switch (cls) {
  case 2: sec_idx = obj_find_or_add_section(ctx->obj, "?ID?", SEC_IDATA, 1); break;
  case 4: sec_idx = obj_find_or_add_section(ctx->obj, "?XD?", SEC_XDATA, 1); break;
  default: /* EDATA 现状 */;
}
```

- 注意 obj.c:227 对 SEC_IDATA 有特殊处理（bytes_len==0 时基址行为），先读确认
- 每个段首次创建时预留自身基址（EDATA=0x80; IDATA 变量放 0x00-0x7F 需避开寄存器文件/位区——确认 sim251 布局; XDATA 基址 0x0000 或 sim251 映射）
- 符号注册: obj_add_symbol 的 value 是段内偏移——isel 用绝对地址时按段基址折算（现 EDATA 是"段内偏移+0x80"模式, 需统一）

- [ ] **步骤 3：isel 寻址分流**

- 查符号所在段 → EDATA: MOV WRj,SYM（现状）; IDATA: 16 位 EDATA 寻址可直达 0x80-0xFF（251 无 SFR 冲突）, 或 @Ri 8 位间接; XDATA: MOVX 序列
- 指针间接: 指针值的存储类属性（ssa.c:999 已继承到 ptr_attr.ctype_data）→ 间接读走 MOVX（xdata 指针）或 @Ri（idata 指针）
- `c251_obj_has_sym` / sym_size 查询需带段信息

- [ ] **步骤 4：MOVX 编码（encode.c）**

```bash
# sim251 已有: 0xE0 MOVX A,@DPTR / 0xF0 MOVX @DPTR,A / 0xE2/0xE3/0xF2/0xF3 MOVX @Ri
# 查 251 扩展: MOVX Rm,@WRj (字节/字) 是否在 decode_impl.inc — 有则用, 无则 @DPTR 序列
grep -n "movx" sim251/src/decode_impl.inc
```

- [ ] **步骤 5：验证**

```bash
python scripts/c251/c251_sim.py test/suite/87_idata.c test/suite/88_xdata_arrays.c test/suite/96_idata_ptr.c
# 期望全 OK (0)
```

- [ ] **步骤 6：Commit**

```bash
git add src/core/c251/c251_gen.c src/core/c251/c251_isel.c src/core/c251/c251_encode.c
git commit -m "feat(c251): 存储类 idata/xdata — 按 ctype_data 选段 + MOVX 寻址 (87/88/96 PASS)"
```

---

### 任务 3：循环后置/前置递减（79_while_nminus / 80_postdec / 81_predec）

**文件：**
- 修改：`src/core/c251/c251_isel.c`（while(n--) 条件/索引 phi 语义）
- 修改：`src/core/ssa.c` 或 `ssa_pass.c`（若 SSA 侧 phi 语义错）

- [ ] **步骤 1：确认语义缺口**

79: `while (n--) g_buf[n] = 0xAA;` n=5 → 期望写 g_buf[4..0]（5 个）=850; 实际 ret=680=4×0xAA（少一次或索引错位）。
80: 同型 ret=680。81: `while (--n)` → 期望写 g_buf[4..1]（4 个）=680; 实际 ret=510=3×0xAA。

```bash
python scripts/c251/c251_sim.py test/suite/79_while_nminus.c   # ret=680
scripts/c251cc.exe -ssa test/suite/79_while_nminus.c | grep -B2 -A4 "n--\|phi\|BR" | head -50
```

- [ ] **步骤 2：根因分析（systematic-debugging）**

- n-- 是后置递减：循环条件用**递减前**值判断 truthy, 循环体内用**递减后**值
- 检查 SSA 中 n-- 的 phi（循环入口值 vs 递减结果）是否被 isel 正确排序（M2.5 批次#3 17f71dd 修过 gcd 的 phi 两阶段并行语义, 同类）
- 检查 `g_buf[n]` 的索引装载是否拿到递减后值, 而 BR 条件是递减前值（或反之）

- [ ] **步骤 3：验证**

```bash
python scripts/c251/c251_sim.py test/suite/79_while_nminus.c test/suite/80_postdec.c test/suite/81_predec.c
# 期望全 OK (79/80=850 语义, 81=680 语义; 测试文件当前无 EXPECT 标注 → ret=0 默认, 需补 /* EXPECT */ 或改断言)
```

> 注：79/80/81 三个测试**没有 EXPECT 注释**, c251_sim.py 默认期望 0 → 当前 "FAIL ret=680 应为 0" 是默认值而非真实期望。修复后**补上 EXPECT 注释**（79/80=850, 81=680）再验收, 防止假绿/假红。

- [ ] **步骤 4：Commit**

```bash
git add src/core/c251/c251_isel.c src/core/ssa.c test/suite/79_while_nminus.c test/suite/80_postdec.c test/suite/81_predec.c
git commit -m "fix(c251): while(n--)/--n 循环 phi 语义 + EXPECT 标注 (79/80/81 PASS)"
```

---

### 任务 4：前端缺口（82_printf_chain / 83_lvalue_addr_protect）

**文件：**
- 修改：`src/core/parser.c`（数组衰减 + 指针算术）
- 修改：`src/core/c251/c251_isel.c`（LHS 寄存器地址保护）

- [ ] **步骤 1：82_printf_chain CE 定位（line 53）**

`const unsigned char *f = fmt + 1;` → "Integer expression expected, but got fmt"。局部数组 `fmt` + 1 的衰减+指针算术在前端失败（M2.5 dcc6148 修过数组衰减就地降级, 此场景仍漏）。最小复现：

```c
int main(void){ unsigned char fmt[6]={1,2,3,4,0,0}; unsigned char *f = fmt + 1; return *f; }  /* 期望 2 */
```

- [ ] **步骤 2：83_lvalue_addr_protect ret=7 定位**

ret=7 = 1|2|4 → t_arr_int_arith(1) / t_arr_ternary(2) / t_arr_postinc_tern(4) 失败; t_arr_mul_deref(8) / t_local_arr_arith(16) 通过。三个失败共用特征：**数组元素写的 LHS 地址物化后, RHS 求值覆写地址寄存器**（2026-08-10 修过同类, 有回归）。逐个子测试 `-asm` 看地址保护是否还在。

- [ ] **步骤 3：验证**

```bash
python scripts/c251/c251_sim.py test/suite/82_printf_chain.c test/suite/83_lvalue_addr_protect.c
# 期望全 OK (0)
```

- [ ] **步骤 4：Commit**

```bash
git add src/core/parser.c src/core/c251/c251_isel.c
git commit -m "fix(c251): 局部数组衰减+指针算术 + LHS 地址保护回归 (82/83 PASS)"
```

---

### 任务 5：已知布局敏感问题（03/05/10 SCON=00 / 96_idata_ptr 单独编译）— 根本修复

**文件：**
- 修改：`src/core/c251/c251_isel.c`（A/寄存器残留——明确加载）

- [ ] **步骤 1：记录现状**

- **03/05/10 SCON=00**：`SCON = (SCON&0x3f)|mode` 生成 `MOV SCON,A` 时 A 未明确加载 → 布局变化触发（功能等价, SCON 最终=0x40, 但 compare_sfr 严格顺序 FAIL）
- **96_idata_ptr 单独编译** R7=3：&ibuf 加载错（布局不稳定）→ Suite 中 PASS（少库布局）

- [ ] **步骤 2：根本修复**

sfr STORE 路径（c251_isel.c `MOV dirdesc,A`）前保证 A 显式加载（值先 MOV A,Rlo 而非依赖残留）；`&sym` 物化路径保证立即数显式装载。目标是**任意布局下行为一致**（消除"布局敏感"）。

- [ ] **步骤 3：验证**

```bash
python scripts/c251/c251_sim.py   # 全量, 无新增 FAIL
python scripts/c251/c251_sim.py test/suite/03_global_vars.c test/suite/05_arith_mul.c test/suite/10_cmp_eq_ne.c test/suite/96_idata_ptr.c
```

- [ ] **步骤 4：Commit**

```bash
git add src/core/c251/c251_isel.c
git commit -m "fix(c251): sfr/地址物化 A 显式加载 — 消除布局敏感 (03/05/10/96 稳定)"
```

---

### 任务 6：M3 验收

**文件：**
- 运行验证

- [ ] **步骤 1：suite 全量**

```bash
python scripts/c251/c251_sim.py
# 期望: 64 OK / 0 FAIL / 37 SKIP（M3 范围 72-99 全绿）; 记录实际
```

- [ ] **步骤 2：execute 全量**

```bash
python scripts/c251/execute_runner.py
# 期望 ≥104 PASS; 记录剩余 FAIL 归类
```

- [ ] **步骤 3：STC32G**

```bash
python tests/c251/stc32g_build.py compile   # 保持 34/34
```

- [ ] **步骤 4：keil_size_compare 更新**

```bash
python scripts/c251/keil_size_compare.py suite
```

- [ ] **步骤 5：结果记录**

追加到本文档末尾（suite/execute/stc32g 数据 + 剩余遗留 + M4/M5 清单）

- [ ] **步骤 6：Commit**

```bash
git add docs/superpowers/plans/2026-08-11-c251-backend-m3.md
git commit -m "docs: M3 验收记录"
```

---

## 自检记录

- **规格覆盖度**：设计规格 M4（sfr/sbit/中断）T1 前已完成（3 提交）+ M3 剩余（far T1、存储类 T2）+ M2.5 遗留（循环后置递减 T3、前端缺口 T4）+ 已知布局敏感（T5）+ 验收（T6）。
- **占位符扫描**：T1/T2 步骤 1 标注"先读 sim251 确认编码/路由"（实现前置动作, 编码权威在 sim251）；无 TODO 占位。
- **类型一致性**：`ctype_data`（parser.c:2006-2011 已有）直接复用；far 指针 size=3 与 Keil 通用指针布局一致；`process_global_var` 段选择与 `obj_add_symbol` 段内偏移模式兼容。
- **遗留（M4/M5）**：32 位 DRk 运算完整、variadic 完整 ABI（stack 传参）、完整 linscan/peephole、STC32G 全部示例、Keil 大小差距收敛。

---

## 当前进展 (2026-08-12)

### 批次 #6 (本次): far 24 位指针 — Suite 64 OK / 0 FAIL 全绿 🎉

**提交**: 4681268 — far 关键字 + 属性下推 + @DRk 寻址 (77_far PASS)

**实现 (Keil XSMALL+Source 通用指针布局参考)**
- parser: `far` 关键字 (ctype_data=7) + 修复 `volatile *`/`far *` cast 解析 (属性循环吞 `*`)
  + **属性下推**: `unsigned char *` 的限定符修饰被指向类型 (原挂在指针上, 解引用丢 unsigned)
- encode: MOV Rm,@DRk (7E (k/4)B (m)0) / MOV @DRk,Rm (7A (k/4)B (m)0)
  / MOV WRj,@DRk (7E (k/4)0A (j/2)0) / MOV @DRk,WRj (7E (k/4)1A (j/2)0)
- isel: is_far_ptr (ctype_data==7) → load_far_addr (24 位常量: 低 16 位 + 空间字节)
  + alloc_far_dr (选 DR0/DR4 对) + INTTOPTR 常量透传

**Keil 参考** (k77.LST): `MOV WR6,#0FE10H; MOV WR4,#07EH; MOV @DR4,R11` →
DR4=0x007EFE10 → @DR4 低 24 位 = 0x7EFE10 → sim251 ld_far8 路由 (XFR 窗口/xram)。
Keil 版 77 在 sim251 上跑通 (ret=0), c251cc 生成同构序列亦 PASS。

**验证**
| 指标 | 批次 #5 后 | 批次 #6 后 |
|------|-----------|-----------|
| Suite | 63 OK / 1 FAIL | **64 OK / 0 FAIL (全绿)** |
| Execute | 104 PASS / 3 FAIL | **104 PASS / 3 FAIL** (0041/0042/0057 旧问题) |

**M3 任务状态**
- ✅ T1 far 指针 (77) / T2 存储类 (87/88/96 语义达成) / T3 循环后置递减 (79/80/81)
- ✅ T4 前端缺口 (82/83) / T5 布局敏感根修 (03/05/10/96 稳定)
- ⏳ T6 M3 验收 (keil_size_compare 更新)
- 遗留 (M4/M5): 32 位 DRk 运算完整 / variadic ABI / WR8-WR14 扩展 / 循环变量钉寄存器

### 批次 #5 (2026-08-12): 寄存器分配对齐 Keil 纪律 — Suite 55→63 OK, 9 FAIL→1

**提交 (3)**
| 提交 | 内容 |
|------|------|
| d3f17fd | 前端: 指针初始化死代码 eval_intexpr 移除 (82 CE 修复, fmt+1 可编译) |
| 205c4ec | isel: 块本地寄存器模型 + def 落槽 + 宽度感知 + phi 多拷贝 (82/83/79/80/81/87/88/96 全过) |
| 65d56e4 | test: 79/80/81 EXPECT 标注 (850/850/680) + Keil 寄存器分配参考文档 |

**根因链 (参考 docs/c251-寄存器分配参考-keil.md)**
1. 跨块寄存器绑定无条件信任 → 强制溢出从物理寄存器读旧绑定值 (b4 溢出 v18 实为 cond) → SELECT 读垃圾槽
2. STORE 间接路径写宽度用值宽度 → `g_buf[i]=31` 16 位写越界到 g_buf[i+1]
3. LOAD 读宽度用 dest 宽度 → `int x=buf[i]` 16 位读越界 (预防性修复)
4. emit_phi_copies 3+ phi 拷贝临时寄存器复用 (prev_tmp 只避连续 2 个) → 覆盖源值

**修复 (对齐 Keil XSMALL+Source 纪律, 2 核心改动)**
- 块入口清空 value_to_reg/reg_val (块本地寄存器模型; 物理寄存器内容只在本块可信)
- global-live 值 def 时立即落槽 (槽权威; 跨块读取走槽)
- STORE/LOAD 宽度按 mem_type (目标/源元素类型) 而非值/dest 宽度
- isel_temp_wr_avoid_list 支持 avoid 列表 (emit_phi_copies 收集已用 WR)

**验证**
| 指标 | 批次 #4 后 | 批次 #5 后 |
|------|-----------|-----------|
| Suite | 55 OK / 9 FAIL | **63 OK / 1 FAIL** (仅 77_far) |
| Execute | 104 PASS / 3 FAIL | **104 PASS / 3 FAIL** (0041/0042/0057 旧问题) |
| 03/05/10 布局敏感 | 依赖布局 | **稳定** (块本地模型消除 A 残留类问题) |
| 96 单独编译 | R7=3 | **PASS** |

**顺带修复**: 87_idata / 88_xdata_arrays / 96_idata_ptr (STORE 宽度修复) + 79/80/81 循环
(块本地模型修复跨块 phi/值传递) — 存储类语义部分意外达成 (当前仍全放 EDATA,
但 idata/xdata 变量经 EDATA 16 位寻址可访问, 测试断言满足; 真正分段留 M4/M5)。

### 批次 #4 (2026-08-12): 12 EEPROM UART2 波特率修复 (commit d985251, 当前分支外)

### 已完成（3 提交, M3 核心）
| 提交 | 功能 | 测试 |
|------|------|------|
| f9a3392 | sfr 声明解析 + MOV A,dir8(0xE5)/MOV dir8,A(0xF5) + sim251 RETVAL | 72/74 PASS |
| 591f3f3 | sbit/bit: RMW（读→清位→OR 位值→写回）+ IROP_LNOT + SRL/SLL Rm 编码 | 73 PASS |
| 506e037 | 中断: interrupt N 后缀语法 + 向量表 0x0003+N*8 + ISR PUSH PSW(0xD0)+R0-R7/POP+RETI | 75/76 PASS |

### 关键验证（Keil 参考）
- **sim251/tests/periph/timer/timer_isr.c**：c251cc 编译, P1 写回 0x84+（中断触发 130+ 次）✓
- **75 死循环根因**：ISR 无 PSW/寄存器保存 → 中断破坏 main 状态; 加 Keil 式保存后修复
- **中断向量**：VEC_T0=0x0B（sim251 mcs251.h:56）, interrupt N → 0x0003+N*8

### 当前基线 (2026-08-12)
| 指标 | 数值 |
|------|------|
| Suite 全量 | **55 OK / 9 FAIL / 37 SKIP**（101 个测试） |
| Suite M3 范围 (72-99, 27 个) | **18 OK / 9 FAIL** |
| Execute | **104 PASS / 3 FAIL / 0 CE** |
| STC32G | 历史基线 34/34（批次 #4 时代）——当前分支无验证脚本, 待恢复 runner 后复验 |

### M3 范围 OK (18)
72_sfr_port_io, 73_sbit_bit_ops, 74_gpio_modes, 75_timer0_irq, 76_isr_reg_save,
84_volatile_sfr_cmp, 85_interrupt_using, 86_sbit_expr, 89_code_arrays, 90_multi_isr,
91_sfr_copy_chain, 92_volatile_loop_sfr, 93_isr_volatile_counter, 94_sfr_sbit_combo,
95_sfr_math, 97_sfr_mask_write, 98_volatile_barrier, 99_sbit_write_read_modify

### M3 范围 FAIL (9) — 任务归属
| 测试 | 结果 | 根因方向 | 任务 |
|------|------|---------|------|
| 77_far_mem | CE: 'far 关键字未解析 | far 24 位指针 + XFR 0x7EFE/xram 寻址 | T1 |
| 79_while_nminus | ret=680 (应 850) | while(n--) 后置递减 phi 语义 | T3 |
| 80_postdec | ret=680 (应 850) | 同上 | T3 |
| 81_predec | ret=510 (应 680) | --n 前置递减 phi 语义 | T3 |
| 82_printf_chain | CE line 53: fmt+1 | 局部数组衰减+指针算术（前端） | T4 |
| 83_lvalue_addr_protect | ret=7 (子测 1/2/3 失败) | LHS 地址保护回归 | T4 |
| 87_idata | ret=2 (iarr[0] 写读不一致) | 存储类 idata 寻址 | T2 |
| 88_xdata_arrays | ret=2 (arr[0] 失败) | 存储类 xdata + MOVX | T2 |
| 96_idata_ptr | ret=1 (*p 读错) | idata 指针间接 | T2 |

### Execute 3 FAIL（旧问题, 归档方向）
- 0041-queen (ret=1): 复杂递归+数组计算
- 0042-prime (死循环): DIV 有符号 R0 冲突 ×4 修复回归
- 0057-duff (ret=1): Duff SSA

### 已知问题（布局敏感, 待根本修复 T5）
- **03/05/10 SCON=00**: `SCON=(SCON&0x3f)|mode` 生成 MOV SCON,A 时 A 未明确加载 → 布局变化触发（功能等价但 compare_sfr 严格顺序 FAIL）
- **96_idata_ptr 单独编译 (R7=3)**: &ibuf 加载错（布局不稳定）→ Suite 中 PASS（少库布局）
- 根本: A 残留/寄存器残留依赖布局 → 任何代码变化都可能触发 → 需明确寄存器加载

### 调试教训
- **编译器崩溃 0xC0000005**: 调试打印（get_tok_str 链接期 sym 失效）导致, 非修复本身
- **relocate() = 0**: C251 reloc 应用路径特殊（relocate_sections 未遍历 text_section reloc）, 垃圾是重排内 reloc 映射写入
- **compare_sfr 的 TCC 事件数大** (68562): 程序循环写 SFR（正常跑马灯）, 去重后 PASS

### 验证命令
- 编译: `cmd //c "build\\build.bat"`（或 scripts/build_compiler.bat 双后端）
- Suite: `python scripts/c251/c251_sim.py`（c251 后端全量; M3 过滤: `--filter "7[2-9]|8[0-9]|9[0-9]"`）
- Execute: `python scripts/c251/execute_runner.py`
- STC32G: `python tests/c251/stc32g_build.py compile`
- Keil 对比: `python scripts/c251/keil_size_compare.py suite`
