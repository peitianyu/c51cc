{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) 启动",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/a.out",
            "args": ["${workspaceFolder}/base/sample/test.c", "--dump-ast"],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "preLaunchTask": "build",
        }
    ]
}